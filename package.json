{
  "name": "kuma-pack-tsreact",
  "version": "0.0.1-alpha",
  "description": "TypeScript + React + Vite skill pack for Kuma Code",
  "license": "MIT",
  "type": "module",
  "private": false,
  "scripts": {
    "chunk": "bun run scripts/chunk.ts",
    "embed": "bun run scripts/embed.ts",
    "index": "bun run scripts/index.ts",
    "package": "bun run scripts/package.ts",
    "build": "bun run chunk && bun run embed && bun run index && bun run package"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "archiver": "^7.0.1"
  },
  "devDependencies": {
    "@types/archiver": "^7.0.0",
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}
